{
    "name": "my-chrome-extension",
    "version": "1.0.0",
    "description": "A simple Chrome extension to display input data.",
    "scripts": {
        "build": "tsc && copyfiles -u 1 src/manifest.json dist/",
        "start": "npx http-server ./dist -p 9090"
    },
    "devDependencies": {
        "@types/chrome": "^0.0.143",
        "copyfiles": "^2.4.1",
        "http-server": "^14.0.0",
        "typescript": "^4.0.0"
    }
}